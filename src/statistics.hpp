#ifndef _statistics_h_INCLUDED
#define _statistics_h_INCLUDED

// clang-format off

//           NAME,            VERBOSITY (0-3), REFERENCE, PRINTING, SYMBOL
//      length 15,  (Always/!0/verbose/never), (relative/percent/MACRO)
#define CADICAL_STATISTICS \
\
  STATISTIC (backbone_phases,        2, INTERVAL, interval, "") \
  STATISTIC (backbone_probes,        1, percent, vars, "%") \
  STATISTIC (backbone_rounds,        2, relative, backbone_phases, "per") \
  STATISTIC (backbone_units,         1, relative, backbone_phases, "per") \
  STATISTIC (backtracked,            3, NO_PRINTING, 0, 0) \
  STATISTIC (blocked,                1, percent, clause_added_irr, "%") \
  STATISTIC (blocked_candidates,     2, relative, blockings, "per") \
  STATISTIC (blocked_pure_clauses,   2, percent, blocked_pure_literals, "per") \
  STATISTIC (blocked_pure_literals,  2, percent, vars, "%") \
  STATISTIC (blocked_resolutions,    2, relative, blocked_candidates, "per") \
  STATISTIC (blockings,              2, INTERVAL, interval, "") \
  STATISTIC (clause_added_irr,       2, percent, clause_added_total, "%") \
  STATISTIC (clause_added_red,       2, percent, clause_added_total, "%") \
  STATISTIC (clause_added_total,     2, relative, searches, "per") \
  STATISTIC (clause_current_irr,     3, NO_PRINTING, 0, 0) \
  STATISTIC (clause_current_red,     3, NO_PRINTING, 0, 0) \
  STATISTIC (clause_current_total,   3, NO_PRINTING, 0, 0) \
  STATISTIC (clause_improved_glue,   2, relative, propagations, "per") \
  STATISTIC (clause_promoted_tier1,  2, relative, propagations, "per") \
  STATISTIC (clause_promoted_tier2,  2, relative, propagations, "per") \
  STATISTIC (clause_recompute_glue,  2, relative, propagations, "per") \
  STATISTIC (collected,              1, relative, propagations, "per") \
  STATISTIC (collections,            2, relative, propagations, "per") \
  STATISTIC (compacts,               1, relative, propagations, "per") \
  STATISTIC (condition_active,       2, relative, propagations, "per") \
  STATISTIC (condition_candidates,   2, relative, propagations, "per") \
  STATISTIC (conditioned,            1, relative, propagations, "per") \
  STATISTIC (conditioned_assign,     2, relative, propagations, "per") \
  STATISTIC (conditioned_autarky,    2, relative, propagations, "per") \
  STATISTIC (conditioned_cond,       2, relative, propagations, "per") \
  STATISTIC (conditioned_initial,    2, relative, propagations, "per") \
  STATISTIC (conditionings,          2, relative, propagations, "per") \
  STATISTIC (condition_pre_assign,   2, relative, propagations, "per") \
  STATISTIC (condition_pre_autarky,  2, relative, propagations, "per") \
  STATISTIC (condition_pre_cond,     2, relative, propagations, "per") \
  STATISTIC (condition_propagated,   2, relative, propagations, "per") \
  STATISTIC (conflicts,              0, relative, propagations, "per") \
  STATISTIC (conflicts_chrono,       2, relative, propagations, "per") \
  STATISTIC (congruence_ands,        2, relative, propagations, "per") \
  STATISTIC (congruence_dummy_ands,  2, relative, propagations, "per") \
  STATISTIC (congruence_gates,       2, relative, propagations, "per") \
  STATISTIC (congruence_gates_and,   2, relative, propagations, "per") \
  STATISTIC (congruence_gates_ite,   2, relative, propagations, "per") \
  STATISTIC (congruence_gates_xor,   2, relative, propagations, "per") \
  STATISTIC (congruence_ites,        2, relative, propagations, "per") \
  STATISTIC (congruence_rounds,      2, INTERVAL, interval, "") \
  STATISTIC (congruence_rw_ands,     2, relative, propagations, "per") \
  STATISTIC (congruence_rw_ites,     2, relative, propagations, "per") \
  STATISTIC (congruence_rw_xors,     2, relative, propagations, "per") \
  STATISTIC (congruence_simp_ands,   2, relative, propagations, "per") \
  STATISTIC (congruence_simp_ites,   2, relative, propagations, "per") \
  STATISTIC (congruence_simplified,  2, relative, propagations, "per") \
  STATISTIC (congruence_simp_xors,   2, relative, propagations, "per") \
  STATISTIC (congruence_subsumed,    2, relative, propagations, "per") \
  STATISTIC (congruence_trite,       2, relative, propagations, "per") \
  STATISTIC (congruence_unary,       2, relative, propagations, "per") \
  STATISTIC (congruence_unary_and,   2, relative, propagations, "per") \
  STATISTIC (congruence_unary_ite,   2, relative, propagations, "per") \
  STATISTIC (congruence_units,       2, relative, propagations, "per") \
  STATISTIC (congruence_xors,        2, relative, propagations, "per") \
  STATISTIC (congruent,              1, relative, propagations, "per") \
  STATISTIC (cover_asymmetric,       2, relative, propagations, "per") \
  STATISTIC (cover_blocked,          2, relative, propagations, "per") \
  STATISTIC (coverings,              2, relative, propagations, "per") \
  STATISTIC (cover_total,            1, relative, propagations, "per") \
  STATISTIC (decision_random,        2, relative, propagations, "per") \
  STATISTIC (decision_random_phase,  2, relative, propagations, "per") \
  STATISTIC (decisions,              0, relative, propagations, "per") \
  STATISTIC (decision_searched,      2, relative, propagations, "per") \
  STATISTIC (decompositions,         1, relative, propagations, "per") \
  STATISTIC (deduplicated,           1, relative, propagations, "per") \
  STATISTIC (deduplicate_init,       2, relative, propagations, "per") \
  STATISTIC (deduplicate_rounds,     2, relative, propagations, "per") \
  STATISTIC (deduplicate_unary,      2, relative, propagations, "per") \
  STATISTIC (deduplications,         2, relative, propagations, "per") \
  STATISTIC (eager_subsumed,         1, relative, propagations, "per") \
  STATISTIC (eager_subsumtions,      2, relative, propagations, "per") \
  STATISTIC (eliminate_and,          2, relative, propagations, "per") \
  STATISTIC (eliminate_complete,     2, relative, propagations, "per") \
  STATISTIC (eliminated,             1, relative, propagations, "per") \
  STATISTIC (eliminated_and,         2, relative, propagations, "per") \
  STATISTIC (eliminated_defs,        2, relative, propagations, "per") \
  STATISTIC (eliminate_def_check,    2, relative, propagations, "per") \
  STATISTIC (eliminate_def_success,  2, relative, propagations, "per") \
  STATISTIC (eliminate_def_ticks,    2, relative, propagations, "per") \
  STATISTIC (eliminate_def_unit,     2, relative, propagations, "per") \
  STATISTIC (eliminated_equivalent,  2, relative, propagations, "per") \
  STATISTIC (eliminated_ite,         2, relative, propagations, "per") \
  STATISTIC (eliminated_xor,         2, relative, propagations, "per") \
  STATISTIC (eliminate_equivalence,  2, relative, propagations, "per") \
  STATISTIC (eliminate_fast_phases,  2, relative, propagations, "per") \
  STATISTIC (eliminate_fast_rounds,  2, relative, propagations, "per") \
  STATISTIC (eliminate_gates,        2, relative, propagations, "per") \
  STATISTIC (eliminate_ite,          2, relative, propagations, "per") \
  STATISTIC (eliminate_otf_str,      2, relative, propagations, "per") \
  STATISTIC (eliminate_otf_sub,      2, relative, propagations, "per") \
  STATISTIC (eliminate_phases,       2, relative, propagations, "per") \
  STATISTIC (eliminate_resolved,     2, relative, propagations, "per") \
  STATISTIC (eliminate_strength_bw,  2, relative, propagations, "per") \
  STATISTIC (eliminate_substituted,  2, relative, propagations, "per") \
  STATISTIC (eliminate_subsumed_bw,  2, relative, propagations, "per") \
  STATISTIC (eliminate_tried,        2, relative, propagations, "per") \
  STATISTIC (eliminate_tried_res,    2, relative, propagations, "per") \
  STATISTIC (eliminate_xor,          2, relative, propagations, "per") \
  STATISTIC (eliminations,           2, relative, propagations, "per") \
  STATISTIC (extended,               1, relative, propagations, "per") \
  STATISTIC (extensions,             1, relative, propagations, "per") \
  STATISTIC (factor_added_clauses,   2, relative, propagations, "per") \
  STATISTIC (factor_added_literals,  2, relative, propagations, "per") \
  STATISTIC (factored,               1, relative, propagations, "per") \
  STATISTIC (factored_and,           2, relative, propagations, "per") \
  STATISTIC (factored_eliminated,    2, relative, propagations, "per") \
  STATISTIC (factored_ite,           2, relative, propagations, "per") \
  STATISTIC (factored_xor,           2, relative, propagations, "per") \
  STATISTIC (factorings,             1, relative, propagations, "per") \
  STATISTIC (factor_removed_irr,     2, relative, propagations, "per") \
  STATISTIC (factor_removed_lits,    2, relative, propagations, "per") \
  STATISTIC (factor_removed_red,     2, relative, propagations, "per") \
  STATISTIC (failed_literals,        1, relative, propagations, "per") \
  STATISTIC (flush_hyper,            2, relative, propagations, "per") \
  STATISTIC (flushings,              1, relative, propagations, "per") \
  STATISTIC (flush_learned,          1, relative, propagations, "per") \
  STATISTIC (garbage_bytes,          3, relative, propagations, "per") \
  STATISTIC (garbage_clauses,        3, relative, propagations, "per") \
  STATISTIC (garbage_literals,       3, relative, propagations, "per") \
  STATISTIC (hbr_redundant,          2, relative, propagations, "per") \
  STATISTIC (hbrs,                   1, relative, propagations, "per") \
  STATISTIC (hbr_sizes,              2, relative, propagations, "per") \
  STATISTIC (hbr_subsuming,          2, relative, propagations, "per") \
  STATISTIC (ilb_reuse_assumptions,  2, relative, propagations, "per") \
  STATISTIC (ilb_reuse_levels,       2, relative, propagations, "per") \
  STATISTIC (ilb_reuse_literals,     2, relative, propagations, "per") \
  STATISTIC (ilb_success,            1, relative, propagations, "per") \
  STATISTIC (ilb_triggers,           2, relative, propagations, "per") \
  STATISTIC (incremental_decay,      1, relative, propagations, "per") \
  STATISTIC (inprobingphases,        1, relative, propagations, "per") \
  STATISTIC (inprobingsuccess,       1, relative, propagations, "per") \
  STATISTIC (instantiated,           1, relative, propagations, "per") \
  STATISTIC (instantiate_tried,      2, relative, propagations, "per") \
  STATISTIC (instantiations,         1, relative, propagations, "per") \
  STATISTIC (irredundant_literals,   3, relative, propagations, "per") \
  STATISTIC (learned_binaries,       2, relative, propagations, "per") \
  STATISTIC (learned_clauses,        2, relative, propagations, "per") \
  STATISTIC (learned_literals,       2, relative, propagations, "per") \
  STATISTIC (learned_units,          2, relative, propagations, "per") \
  STATISTIC (lucky,                  1, relative, propagations, "per") \
  STATISTIC (lucky_backward_one,     2, relative, propagations, "per") \
  STATISTIC (lucky_backward_zero,    2, relative, propagations, "per") \
  STATISTIC (lucky_constant_one,     2, relative, propagations, "per") \
  STATISTIC (lucky_constant_zero,    2, relative, propagations, "per") \
  STATISTIC (lucky_forward_one,      2, relative, propagations, "per") \
  STATISTIC (lucky_forward_zero,     2, relative, propagations, "per") \
  STATISTIC (lucky_random,           2, relative, propagations, "per") \
  STATISTIC (lucky_tried,            2, relative, propagations, "per") \
  STATISTIC (lucky_units,            2, relative, propagations, "per") \
  STATISTIC (mark_block,             3, relative, propagations, "per") \
  STATISTIC (mark_elim,              3, relative, propagations, "per") \
  STATISTIC (mark_factor,            3, relative, propagations, "per") \
  STATISTIC (mark_subsume,           3, relative, propagations, "per") \
  STATISTIC (mark_ternary,           3, relative, propagations, "per") \
  STATISTIC (minimized,              1, relative, propagations, "per") \
  STATISTIC (otfs_strengthened,      1, relative, propagations, "per") \
  STATISTIC (otfs_subsumed,          1, relative, propagations, "per") \
  STATISTIC (preprocessings,         2, relative, propagations, "per") \
  STATISTIC (probed,                 1, relative, propagations, "per") \
  STATISTIC (probe_failed_literals,  1, relative, propagations, "per") \
  STATISTIC (probingrounds,          2, relative, propagations, "per") \
  STATISTIC (propagations,           0, PER_SECOND, second, "per") \
  STATISTIC (propagations_backbone,  2, relative, propagations, "per") \
  STATISTIC (propagations_cover,     2, relative, propagations, "per") \
  STATISTIC (propagations_inst,      2, relative, propagations, "per") \
  STATISTIC (propagations_probe,     2, relative, propagations, "per") \
  STATISTIC (propagations_search,    2, relative, propagations, "per") \
  STATISTIC (propagations_transred,  2, relative, propagations, "per") \
  STATISTIC (propagations_vivify,    2, relative, propagations, "per") \
  STATISTIC (recomputed_tiers,       1, relative, propagations, "per") \
  STATISTIC (reduced,                1, relative, propagations, "per") \
  STATISTIC (reductions,             1, relative, propagations, "per") \
  STATISTIC (rephased,               1, relative, propagations, "per") \
  STATISTIC (rephased_best,          2, relative, propagations, "per") \
  STATISTIC (rephased_flipped,       2, relative, propagations, "per") \
  STATISTIC (rephased_inverted,      2, relative, propagations, "per") \
  STATISTIC (rephased_original,      2, relative, propagations, "per") \
  STATISTIC (rephased_random,        2, relative, propagations, "per") \
  STATISTIC (rephased_walk,          2, relative, propagations, "per") \
  STATISTIC (restart,                1, relative, propagations, "per") \
  STATISTIC (restart_levels,         2, relative, propagations, "per") \
  STATISTIC (restart_stable,         2, relative, propagations, "per") \
  STATISTIC (restorations,           1, relative, propagations, "per") \
  STATISTIC (restored_clauses,       1, relative, propagations, "per") \
  STATISTIC (restored_literals,      2, relative, propagations, "per") \
  STATISTIC (reused,                 1, relative, propagations, "per") \
  STATISTIC (reused_levels,          2, relative, propagations, "per") \
  STATISTIC (reused_stable,          2, relative, propagations, "per") \
  STATISTIC (scores_rescored,        2, relative, propagations, "per") \
  STATISTIC (scores_shuffled,        2, relative, propagations, "per") \
  STATISTIC (searches,               2, relative, propagations, "per") \
  STATISTIC (sections,               3, relative, propagations, "per") \
  STATISTIC (shrunken,               1, relative, propagations, "per") \
  STATISTIC (shrunken_minimize,      1, relative, propagations, "per") \
  STATISTIC (stable_conflicts,       2, relative, propagations, "per") \
  STATISTIC (stable_phases_current,  2, relative, propagations, "per") \
  STATISTIC (stable_phases_total,    1, relative, propagations, "per") \
  STATISTIC (strengthened,           1, relative, propagations, "per") \
  STATISTIC (subsume_checks,         2, relative, propagations, "per") \
  STATISTIC (subsume_checks_binary,  2, relative, propagations, "per") \
  STATISTIC (subsumed,               1, relative, propagations, "per") \
  STATISTIC (subsumed_irredundant,   2, relative, propagations, "per") \
  STATISTIC (subsumed_redundant,     2, relative, propagations, "per") \
  STATISTIC (subsume_phases,         2, relative, propagations, "per") \
  STATISTIC (subsume_rounds,         2, relative, propagations, "per") \
  STATISTIC (subsume_tried,          2, relative, propagations, "per") \
  STATISTIC (sweep,                  1, relative, propagations, "per") \
  STATISTIC (sweep_bb_fixed,         2, relative, propagations, "per") \
  STATISTIC (sweep_bb_flip,          2, relative, propagations, "per") \
  STATISTIC (sweep_bb_flipped,       2, relative, propagations, "per") \
  STATISTIC (sweep_bb_solved,        2, relative, propagations, "per") \
  STATISTIC (sweep_bb_solved_sat,    2, relative, propagations, "per") \
  STATISTIC (sweep_bb_solved_to,     2, relative, propagations, "per") \
  STATISTIC (sweep_bb_solved_unsat,  2, relative, propagations, "per") \
  STATISTIC (sweep_clauses,          2, relative, propagations, "per") \
  STATISTIC (sweep_completed,        2, relative, propagations, "per") \
  STATISTIC (sweep_depth,            2, relative, propagations, "per") \
  STATISTIC (sweep_environment,      2, relative, propagations, "per") \
  STATISTIC (sweep_eq,               1, relative, propagations, "per") \
  STATISTIC (sweep_eq_flip,          2, relative, propagations, "per") \
  STATISTIC (sweep_eq_flipped,       2, relative, propagations, "per") \
  STATISTIC (sweep_eq_solved,        2, relative, propagations, "per") \
  STATISTIC (sweep_eq_solved_sat,    2, relative, propagations, "per") \
  STATISTIC (sweep_eq_solved_to,     2, relative, propagations, "per") \
  STATISTIC (sweep_eq_solved_unsat,  2, relative, propagations, "per") \
  STATISTIC (sweep_solved,           2, relative, propagations, "per") \
  STATISTIC (sweep_solved_sat,       2, relative, propagations, "per") \
  STATISTIC (sweep_solved_unsat,     2, relative, propagations, "per") \
  STATISTIC (sweep_units,            1, relative, propagations, "per") \
  STATISTIC (sweep_variables,        2, relative, propagations, "per") \
  STATISTIC (ternary,                1, relative, propagations, "per") \
  STATISTIC (ternary_htrs,           1, relative, propagations, "per") \
  STATISTIC (ternary_htrs_binary,    2, relative, propagations, "per") \
  STATISTIC (ternary_htrs_ternary,   2, relative, propagations, "per") \
  STATISTIC (ternary_resolutions,    2, relative, propagations, "per") \
  STATISTIC (ticks,                  0, relative, propagations, "per") \
  STATISTIC (ticks_backbone,         2, relative, propagations, "per") \
  STATISTIC (ticks_factor,           2, relative, propagations, "per") \
  STATISTIC (ticks_probe,            2, relative, propagations, "per") \
  STATISTIC (ticks_search_stable,    2, relative, propagations, "per") \
  STATISTIC (ticks_search_unstable,  2, relative, propagations, "per") \
  STATISTIC (ticks_sweep,            2, relative, propagations, "per") \
  STATISTIC (ticks_ternary,          2, relative, propagations, "per") \
  STATISTIC (ticks_vivify,           2, relative, propagations, "per") \
  STATISTIC (ticks_walk,             2, relative, propagations, "per") \
  STATISTIC (ticks_walk_break,       2, relative, propagations, "per") \
  STATISTIC (ticks_walk_flip,        2, relative, propagations, "per") \
  STATISTIC (ticks_walk_flip_broke,  2, relative, propagations, "per") \
  STATISTIC (ticks_walk_flip_wl,     2, relative, propagations, "per") \
  STATISTIC (ticks_walk_pick,        2, relative, propagations, "per") \
  STATISTIC (transitive_clauses,     1, relative, propagations, "per") \
  STATISTIC (transitive_rounds,      2, relative, propagations, "per") \
  STATISTIC (transitive_units,       2, relative, propagations, "per") \
  STATISTIC (up_cb,                  2, relative, propagations, "per") \
  STATISTIC (up_cb_add,              1, relative, propagations, "per") \
  STATISTIC (up_cb_check_model,      1, relative, propagations, "per") \
  STATISTIC (up_cb_prop,             1, relative, propagations, "per") \
  STATISTIC (up_cb_prop_assign,      2, relative, propagations, "per") \
  STATISTIC (up_cb_prop_clash,       2, relative, propagations, "per") \
  STATISTIC (up_cb_prop_explain,     2, relative, propagations, "per") \
  STATISTIC (up_learn,               1, relative, propagations, "per") \
  STATISTIC (up_learn_conflict,      2, relative, propagations, "per") \
  STATISTIC (up_learn_elevating,     2, relative, propagations, "per") \
  STATISTIC (up_learn_empty,         2, relative, propagations, "per") \
  STATISTIC (up_learn_lazy_elevate,  2, relative, propagations, "per") \
  STATISTIC (up_learn_out_of_order,  2, relative, propagations, "per") \
  STATISTIC (up_learn_propagating,   2, relative, propagations, "per") \
  STATISTIC (up_learn_unit,          2, relative, propagations, "per") \
  STATISTIC (variables_extension,    2, relative, propagations, "per") \
  STATISTIC (variables_original,     1, relative, propagations, "per") \
  STATISTIC (vars,                   3, relative, propagations, "per") \
  STATISTIC (vars_active,            2, relative, propagations, "per") \
  STATISTIC (vars_all_elim,          2, relative, propagations, "per") \
  STATISTIC (vars_all_elim_fast,     2, relative, propagations, "per") \
  STATISTIC (vars_all_fixed,         2, relative, propagations, "per") \
  STATISTIC (vars_all_pure,          2, relative, propagations, "per") \
  STATISTIC (vars_all_substituted,   2, relative, propagations, "per") \
  STATISTIC (vars_bumped,            2, relative, propagations, "per") \
  STATISTIC (vars_declared,          2, relative, propagations, "per") \
  STATISTIC (vars_inactive,          2, relative, propagations, "per") \
  STATISTIC (vars_now_eliminated,    3, relative, propagations, "per") \
  STATISTIC (vars_now_fixed,         3, relative, propagations, "per") \
  STATISTIC (vars_now_pure,          3, relative, propagations, "per") \
  STATISTIC (vars_now_substituted,   3, relative, propagations, "per") \
  STATISTIC (vars_reactivated,       2, relative, propagations, "per") \
  STATISTIC (vars_unused,            2, relative, propagations, "per") \
  STATISTIC (vivifications,          1, relative, propagations, "per") \
  STATISTIC (vivified,               1, relative, propagations, "per") \
  STATISTIC (vivified_irr,           2, relative, propagations, "per") \
  STATISTIC (vivified_tier1,         2, relative, propagations, "per") \
  STATISTIC (vivified_tier2,         2, relative, propagations, "per") \
  STATISTIC (vivified_tier3,         2, relative, propagations, "per") \
  STATISTIC (vivify_checks,          2, relative, propagations, "per") \
  STATISTIC (vivify_decisions,       2, relative, propagations, "per") \
  STATISTIC (vivify_demote,          2, relative, propagations, "per") \
  STATISTIC (vivify_flushed,         2, relative, propagations, "per") \
  STATISTIC (vivify_implied,         2, relative, propagations, "per") \
  STATISTIC (vivify_instantiated,    2, relative, propagations, "per") \
  STATISTIC (vivify_reused,          2, relative, propagations, "per") \
  STATISTIC (vivify_scheduled,       2, relative, propagations, "per") \
  STATISTIC (vivify_strength,        2, relative, propagations, "per") \
  STATISTIC (vivify_strength_irr,    2, relative, propagations, "per") \
  STATISTIC (vivify_strength_tier1,  2, relative, propagations, "per") \
  STATISTIC (vivify_strength_tier2,  2, relative, propagations, "per") \
  STATISTIC (vivify_strength_tier3,  2, relative, propagations, "per") \
  STATISTIC (vivify_subsumed,        2, relative, propagations, "per") \
  STATISTIC (vivify_subsumed_irr,    2, relative, propagations, "per") \
  STATISTIC (vivify_subsumed_red,    2, relative, propagations, "per") \
  STATISTIC (vivify_units,           2, relative, propagations, "per") \
  STATISTIC (walk,                   1, relative, propagations, "per") \
  STATISTIC (walk_broken,            2, relative, propagations, "per") \
  STATISTIC (walk_flips,             2, relative, propagations, "per") \
  STATISTIC (walk_improved,          2, relative, propagations, "per") \
  STATISTIC (walk_sideways,          2, relative, propagations, "per") \
  STATISTIC (walk_warmup,            2, relative, propagations, "per") \
  STATISTIC (walk_warmup_conflicts,  2, relative, propagations, "per") \
  STATISTIC (walk_warmup_decision,   2, relative, propagations, "per") \
  STATISTIC (walk_warmup_dummy,      2, relative, propagations, "per") \
  STATISTIC (walk_warmup_propagate,  2, relative, propagations, "per") \
  STATISTIC (walk_weight_reducing,   2, relative, propagations, "per") \
  STATISTIC (walk_weight_transfer,   2, relative, propagations, "per") \
  STATISTIC (weakened,               1, relative, propagations, "per") \
  STATISTIC (weakened_lengths,       2, relative, propagations, "per")

// clang-format on

#endif
